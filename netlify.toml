# exemplo de um netlify.toml
[build]
  command = "vite build"
  functions = "netlify/functions"
  publish = "dist"

# Redirecionamento de todas as rotas para /index.html
[[redirects]]
  from = "/backend/*"
  to = "/index.html"
  status = 200